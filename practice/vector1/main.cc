#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;


bool descendingComp(const int &a, const int &b){
    if(a > b)
        return true;
    else
        return false;
}


int main(){
    
    int n, order_type;
    vector<int> vec;
    cin >> n;
    cin >> order_type;

    //-----------------------------------------------------------//
    // TODO: Fill your code
       
    //-----------------------------------------------------------//
    
 
}
